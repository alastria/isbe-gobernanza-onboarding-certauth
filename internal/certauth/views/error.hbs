{{template "partials/preamble"}}

    {{template "partials/logobar"}}

    <div class="ui hidden divider"></div>

    <div class="ui container">

        <div class="ui huge header">Ha habido un error</div>

        <p>{{ .message }}</p>

    </div>

{{template "partials/postamble"}}
